version: "2"

run:
  timeout: 3m

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign

formatters:
  enable:
    - gofmt

issues:
  exclude-dirs:
    - vendor
  max-issues-per-linter: 50
  max-same-issues: 5
